# 离线部署完整指南

## ✅ 离线兼容性检查结果

经过全面检查,项目已满足内网离线开发和发布的所有要求:

### 1. 源代码检查 ✅
- **外部依赖**: 无
- **CDN 链接**: 无
- **外部图片**: 已替换为 data URI (SVG 头像)
- **字体文件**: 全部来自 `@fontsource/noto-sans-sc` 本地包

### 2. Mock 数据检查 ✅
- `mockData.ts` 中的 API URL 均为 `localhost:8000`,这些仅用于模拟数据结构,不会实际请求
- 用户头像已从 `picsum.photos` 改为嵌入式 SVG

### 3. 构建产物检查 ✅
- `dist` 目录中无外部资源引用
- 所有 JS/CSS/字体文件均已打包到 `dist/assets`
- Monaco Editor 的 worker 脚本已正确打包
- 仅有代码注释中包含 GitHub 许可证链接(不影响运行)

### 4. 依赖管理 ✅
- 所有依赖均通过 `npm install` 下载到 `node_modules`
- `package-lock.json` 确保依赖版本一致性

---

## 📦 离线迁移步骤

### 在联网电脑上:

1. **安装依赖**
   ```bash
   npm install
   ```

2. **验证构建**(可选但推荐)
   ```bash
   npm run build
   ```
   确认无错误后,检查 `dist` 目录是否生成成功。

3. **打包项目**
   将整个项目文件夹(包含 `node_modules`)压缩:
   ```bash
   # 使用 7-Zip 或 WinRAR 压缩整个文件夹
   # 或使用 PowerShell:
   Compress-Archive -Path . -DestinationPath sp-manager-offline.zip
   ```

### 在内网电脑上:

1. **解压项目**
   ```bash
   # 解压到目标目录
   Expand-Archive -Path sp-manager-offline.zip -DestinationPath C:\projects\sp-manager
   ```

2. **开发模式运行**
   ```bash
   cd C:\projects\sp-manager
   npm run dev
   ```
   访问 `http://localhost:5173`

3. **生产构建**
   ```bash
   npm run build
   ```
   构建产物在 `dist` 目录,可部署到任何静态 Web 服务器。

---

## 🚀 生产部署

### 方式 1: 使用 Nginx
```nginx
server {
    listen 80;
    server_name your-domain.com;
    root /path/to/sp-manager/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

### 方式 2: 使用 Node.js 静态服务器
```bash
# 安装 serve (如果内网有 npm 缓存)
npm install -g serve

# 运行
serve -s dist -p 3000
```

### 方式 3: 使用 Python 简易服务器
```bash
cd dist
python -m http.server 8080
```

---

## ⚠️ 注意事项

1. **Node.js 版本**: 确保内网电脑安装了 Node.js 16+ 版本
2. **网络隔离**: 项目运行时不会发起任何外部网络请求
3. **数据持久化**: 当前使用 Mock 数据,如需连接真实后端,修改 `services/api.ts` 中的 API 地址
4. **浏览器兼容**: 推荐使用 Chrome/Edge 最新版本

---

## 🔧 故障排查

### 问题: 页面空白
- 检查浏览器控制台(F12)是否有错误
- 确认 `npm run dev` 无报错
- 尝试清除浏览器缓存后刷新

### 问题: 字体显示异常
- 确认 `node_modules/@fontsource` 目录存在
- 重新运行 `npm install`

### 问题: Monaco Editor 无法加载
- 确认 `node_modules/monaco-editor` 完整
- 检查 `vite.config.ts` 配置正确

---

## 📝 版本信息

- **Vite**: 5.1.4
- **Vue**: 3.4.21
- **Element Plus**: 2.6.0
- **Monaco Editor**: 0.45.0
- **Node.js**: 推荐 18.x 或 20.x

---

## ✅ 验证清单

在迁移到内网前,请确认:

- [ ] `npm install` 成功完成
- [ ] `npm run dev` 可正常启动
- [ ] `npm run build` 无错误
- [ ] 浏览器访问 `http://localhost:5173` 显示正常
- [ ] 所有页面功能正常(Dashboard, Editor, Search 等)
- [ ] Monaco Editor 语法高亮正常
- [ ] 中文字体显示正常

全部通过后,即可安全打包迁移到内网环境!
